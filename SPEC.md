# 프로젝트: 시니어 일상훈련 인지케어 웹앱 (웰니스 프로토타입)

> 이 문서는 **전체 스펙**이다. 한 번에 전부 구현하지 않는다.
> 작업은 11장 마일스톤 단위로 진행하며, 매 세션마다 **지정된 마일스톤 하나만** 구현한다.
> 각 마일스톤이 끝나면 완료 기준을 직접 확인하고 커밋한 뒤 다음으로 넘어간다.
> 불명확한 점은 합리적인 기본값으로 진행하되 가정을 명시하라.

---

## 1. 제품 개요

고령자(특히 경도인지장애·정상 고령층)를 위한 **일상생활 기반 인지훈련** 웹앱이다. 추상적인 "뇌게임"이 아니라, 장보기·길찾기·요리 레시피·복약 같은 **실제 일상 과제를 게임화**한 훈련을 제공한다. 훈련 수행 데이터는 영역별 "기능 프로파일"로 쌓이고, 가족(자녀)이 볼 수 있는 주간 리포트로 요약된다.

**핵심 차별점 (반드시 구현에 반영):**

1. 훈련 콘텐츠가 곧 실생활 과제다 (전이 효과를 노림).
2. 여러 게임이 **하나의 일상 이야기**로 이어진다 (코스 모드 — 6.5장).
3. 훈련 후 "오늘 실제로 해보기" 미션으로 앱 밖 행동을 유도한다 (실행 브리지).
4. 고령자 접근성을 최우선으로 설계한다 (3장).
5. 가족 연계로 지속 사용 동기를 만든다.

**이것은 웰니스 앱이다 (의료기기 아님).** → 4장 가드레일을 반드시 지켜라.

---

## 2. 기술 스택 / 제약

- **React 18 + Vite**
- **언어: JavaScript. TypeScript를 사용하지 않는다.**
  - 파일 확장자는 `.js` / `.jsx` 만 사용한다.
  - `.ts` / `.tsx` 파일, `tsconfig.json`, `@types/*` 패키지를 만들거나 설치하지 않는다.
- **스타일: Tailwind CSS v3.4** (v4를 설치하지 말 것 — 설정 방식이 달라 충돌한다)
  - 고령자용 접근성 토큰(큰 폰트·큰 터치영역·고대비)을 `tailwind.config.js`에 커스텀 정의해 사용한다.
- **라우팅: react-router-dom**
- **상태관리: Zustand** (가볍게)
- **차트: Recharts** (가족 리포트용)
- **데이터 저장: 브라우저 localStorage만 사용** — 백엔드·서버·계정·로그인 없음.
  - localStorage 접근은 `src/lib/storage.js` 한 곳으로만 감싼다. 다른 파일에서 `localStorage`를 직접 호출하지 않는다.
- **PWA: vite-plugin-pwa** — manifest + service worker, 오프라인 동작, 홈화면 추가.
  - **마일스톤 7에서만 설치한다.** 그 전에 설치하면 개발 중 캐시 때문에 코드 수정이 화면에 반영되지 않는 문제가 발생한다.
- **반응형:** 모바일·태블릿 우선(태블릿에서 특히 보기 좋게), PC도 지원.
- **언어: 모든 UI·콘텐츠는 한국어.**
- **음성 안내: Web Speech API(SpeechSynthesis)** 로 문제·안내를 읽어주는 옵션 제공.
  - **기본값은 꺼짐.** 브라우저·OS에 따라 한국어 음성이 없을 수 있다.
  - **음성이 없으면 진행이 불가능한 기능을 만들지 말 것.** 모든 정보는 화면 텍스트만으로 전달되어야 한다.
- 외부 유료 API·결제·실제 지도 API 사용 금지(길찾기 게임은 자체 그래픽/그리드로 구현).
- **다크모드 구현 금지** (10장 참조).

---

## 3. 고령자 접근성 (최우선 원칙 — 모든 화면에 적용)

- **글자 크기:** 본문 최소 20px, 버튼/선택지 24px 이상, 화면 제목 32px 이상. 설정에서 "글자 크게" 토글(1.25배) 제공.
  - 1.25배 적용 시 버튼 안 글자가 넘치거나 어색하게 줄바꿈되지 않는지 각 마일스톤마다 확인할 것.
- **터치 영역:** 모든 버튼·선택지 최소 높이 64px, 충분한 간격. 작은 아이콘 버튼 금지.
- **대비:** WCAG AAA 지향. 회색 위 회색 텍스트 금지. 배경/텍스트 대비 명확히.
- **내비게이션 단순화:** 하단 큰 탭 3개(홈 · 훈련 · 가족) + 홈에서 설정 진입. 깊은 메뉴 계층 금지. 한 화면에 한 가지 주요 행동.
- **피드백 명확화:** 정답/오답은 색+아이콘+(음성 옵션 시)소리로 즉시. 애니메이션은 절제하고 느리게.
- **오류 관용:** 되돌리기 쉽게, 실수해도 벌점·부정 피드백 최소화. 격려 위주 카피.
- **인지 부하 최소화:** 한 화면에 정보 적게, 진행 단계 표시(예: 3단계 중 1단계).

### 3.1 입력 방식 규칙 (반드시 지킬 것)

- **드래그 앤 드롭 사용 금지.** 손떨림이 있는 고령자에게 드래그는 사실상 사용 불가하며, 마우스와 터치에서 동작이 갈린다.
  - 순서 배열이 필요한 과제는 **탭(클릭)만으로** 가능해야 한다. 구현 방식은 6.4 참조.
- **클릭 가능한 요소는 반드시 `<button>` 태그를 사용한다.** `<div onClick>` 금지 — 키보드로 접근할 수 없다.
- **hover에 정보를 담지 말 것.** 터치 기기에는 hover가 없다. 선택 여부는 반드시 상태 기반 스타일로 표시한다.
- 시맨틱 HTML, `aria-label`, 키보드 조작 가능(Tab 이동 + Enter 실행), 포커스 표시.
- `index.html`에 `<meta name="viewport" content="width=device-width, initial-scale=1">` 포함 (더블탭 확대 방지).

---

## 4. 웰니스 가드레일 (법적으로 중요 — 카피 작성 시 필수)

- **"치매 예방/치료/진단/지연" 같은 의료 효과 주장 금지.** 대신 "두뇌 건강 습관", "일상 능력 지키기", "꾸준한 활동" 같은 웰니스 표현 사용.
- 앱 하단·설정에 고지: *"이 앱은 건강한 생활습관을 돕는 웰니스 서비스이며, 의료적 진단·치료를 대체하지 않습니다."*
  - **이 고지는 마일스톤 2에서 넣는다.** 마지막으로 미루지 않는다.
- 가족 리포트에 "위험/이상/저하 진단" 같은 단정 표현 금지. "이번 주 활동 요약", "꾸준함" 중심의 긍정 프레이밍.

---

## 5. 정보 구조 / 화면 목록

1. **온보딩** — 사용 어르신 이름·나이대 입력, 글자크기·음성 안내 선택. (로컬 저장, 로그인 없음)
2. **홈** — 오늘의 추천 훈련, 연속 사용일(스트릭), 오늘의 "실생활 미션" 카드, 큰 시작 버튼.
3. **훈련 목록** — 코스 모드 + 낱개 게임 카드.
4. **훈련 플레이 화면** — 각 게임별.
5. **훈련 결과 화면** — 잘한 점 격려 + "오늘 실제로 해보기" 실행 브리지 미션 제안.
6. **가족 모드(보호자 리포트)** — 주간 요약 대시보드(같은 localStorage 읽음, 별도 탭/화면).
7. **설정** — 글자 크게, 음성 안내, 데이터 초기화, 웰니스 고지.

---

## 6. 핵심 기능: 일상훈련 게임

각 게임은 **적응형 난이도**(7장 공통 로직)를 따르고, 끝나면 세션 데이터를 저장한다. 각 게임에는 인지 영역 태그가 붙는다.

**구현 순서:** 6.1 장보기 → 6.2 요리 → (완성 후) 6.3 길찾기 → 6.5 코스 모드 → 6.4 복약

### 6.1 장보기 미션 (영역: 작업기억 + 계산) — 마일스톤 3

- 장바구니 목록을 몇 초간 보여준 뒤 가림.
- 가상 마트 진열대(그리드)에서 해당 품목을 터치해 담기.
- (상위 난이도) 계산대에서 "만 원 내면 잔돈은?" 계산.
- **장바구니 목록을 하드코딩하지 말 것.** 6.2의 요리 데이터셋에서 재료를 꺼내 생성한다.
  (예: "오늘은 미역국을 끓입니다" → 목록 = 미역, 소고기, 참기름)
  이렇게 해야 나중에 코스 모드를 붙일 때 수정이 필요 없고, 목록을 외울 이유가 생겨 실생활 과제가 된다.
- 담은 품목은 취소(빼기)가 가능해야 한다. 취소에 벌점 없음.
- 난이도 레버: 목록 길이, 노출 시간, 잔돈 계산 유무, 방해 품목 수.

### 6.2 요리 레시피 미션 (영역: 순서화 + 실행기능 + 작업기억) — 마일스톤 4

- 레시피 단계(예: "쌀 씻기 → 물 붓기 → 30분 불리기 → 밥솥 켜기")를 몇 초간 순서대로 보여준 뒤 가림.
- 뒤섞인 단계 카드를 **올바른 순서로 재배열**하기.
- **구현 방식: 드래그 금지 (3.1 참조).** 다음 중 하나로 만든다.
  - **(권장) 순서대로 고르기:** "1번째로 할 일을 골라주세요" → 탭 → "2번째로..." 순으로 하나씩 선택. 고른 항목은 아래에 순서대로 쌓이고, 잘못 골랐으면 그 항목을 탭해서 뺄 수 있다.
  - (대안) 각 카드 옆 큰 ▲▼ 버튼으로 한 칸씩 이동.
- (상위 난이도) 재료 계량 미니과제("두 컵 반은 몇 컵?") 추가.
- 한국적 소재 사용: 밥·미역국·김치찌개·명절 음식·손주가 좋아하는 반찬 등 친숙한 요리 데이터셋(더미)으로 구성.
  - `src/lib/recipes.js` 에 요리별 `{ id, name, ingredients[], steps[] }` 형태로 정의. 6.1이 이 데이터를 함께 사용한다.
- 끝나면 결과 화면에서 **실행 브리지**로 연결: "오늘 실제로 이 요리 해보기 → 가족과 나눠 먹기" 미션 제안(RealWorldMission으로 저장).
- 난이도 레버: 단계 수, 노출 시간, 방해(오답) 단계 수, 재료 계량 포함 여부.

### 6.3 길찾기 · 버스 순서 (영역: 공간기억 + 순서화) — 마일스톤 8

- 출발→도착 경로 또는 버스 정류장 순서를 잠깐 보여준 뒤, 순서대로 재현.
- 자체 그리드/노선 그래픽으로 구현(외부 지도 API 금지).
- 순서 재현은 6.2와 같이 **탭 기반**으로 구현.
- 난이도 레버: 경유지 수, 노출 시간.

### 6.4 복약 시간 맞추기 (영역: 전망기억 + 실행기능) — 마일스톤 10

- "아침 혈압약, 점심 후 당뇨약, 자기 전 관절약" 스케줄 제시.
- 하루 타임라인(아침/점심/저녁/취침)에 약을 올바르게 배치. **탭으로 선택**(드래그 금지).
- 난이도 레버: 약 개수, 시간대 세분화.
- 이 게임은 6.5 "장 보러 가는 날" 코스에 포함하지 않는다 (일과 리듬이라 결이 다름). 추후 별도 코스에서 다룬다.

### 6.5 코스 모드 — 스토리 연결 (마일스톤 9)

세 게임을 **"장 보러 가는 날"** 하나의 이야기로 묶는다.

```
요리 선택 → 길찾기(마트까지) → 장보기(재료 담기) → 요리(조리 순서) → 실행 브리지
```

**설계 원칙 (반드시 지킬 것):**

- **각 게임은 반드시 독립 실행 가능해야 한다.** 게임 간 데이터 의존 금지.
  스토리는 "포장"이고, 게임 로직은 서로 분리되어 있어야 한다.
- **앞 게임의 실패가 뒤 게임을 막거나 불리하게 만들지 말 것.**
  장보기에서 재료를 놓쳐도 요리 게임은 정상 진행한다. 부족한 재료는 격려 카피와 함께 자동으로 채워준다.
  (예: "미역이랑 참기름을 사오셨네요! 소고기는 냉장고에 있어요")
- **코스 모드에서는 각 게임의 문항 수를 줄여** 전체 7~8분 이내로 한다. 낱개 플레이는 정상 분량.
- **코스 중간에 나가도 진행한 게임까지는 세션에 기록한다.**
- 홈·훈련 목록에서 **코스와 낱개 훈련을 모두** 선택할 수 있다.

---

## 7. 적응형 난이도 & 기능 프로파일

- 각 세션 저장: `{ id, date, gameId, courseId, domain, level, score, accuracy, durationSec }`.
- **적응형 로직:** 영역별 최근 성적으로 다음 난이도 조정 — 정확도 높고 빠르면 레벨 업, 낮으면 레벨 다운. 너무 쉽거나 어렵지 않게(플로우 유지).
  - 난이도 조정은 **조용히** 이루어진다. "난이도를 낮췄습니다" 같은 문구를 노출하지 않는다. 긍정 신호만 보여준다.
- **기능 프로파일:** 영역별 최근 수행을 0~100 점수로 집계 → 홈과 가족 리포트에서 레이더/막대 차트로 시각화.
  - 영역 목록: 작업기억 · 계산 · 전망기억 · 공간기억 · 순서화 · 실행기능
  - **중요:** 차트는 **실제 세션 데이터가 있는 영역만** 축으로 그린다. 영역 목록을 차트 코드에 고정하지 말 것.
    (게임을 순차적으로 추가하는 구조이므로, 아직 해당 게임이 없는 영역이 0점 축으로 표시되면 "못한다"는 오해를 준다. 게임이 추가되면 축이 자동으로 늘어나야 한다.)
  - 축이 3개 이하면 막대 차트, 4개 이상이면 레이더 차트를 사용한다.
- **주의:** 프로파일은 "훈련 활동 요약"으로 표현하고, 의료적 판정으로 표현하지 말 것(4장).

---

## 8. 가족 모드 (보호자 주간 리포트)

같은 기기의 localStorage를 읽어 별도 화면으로 렌더(로그인·서버 없음, 데모용).

- 이번 주 훈련 완료 일수(스트릭), 총 세션 수.
- 영역별 기능 프로파일 차트(추이 포함).
- 실행 미션 수행 여부("오늘 실제로 해보기" 완료 현황 — 예: 완성한 요리 미션).
- "이번 주 하이라이트"(가장 잘한 훈련·꾸준함) 카드.
- **긍정 프레이밍:** "이상 신호" 대신 "이번 주 하이라이트 / 꾸준함" 중심. 단정적 의료 표현 금지.

---

## 9. 데이터 모델

> ※ 아래는 데이터의 **모양**을 설명하기 위한 참고용 표기다.
> 실제 코드는 **JavaScript**로 작성하며, TypeScript 문법을 쓰지 않는다.
> 모든 접근은 `src/lib/storage.js` 를 통한다.

```
UserProfile      { name, ageBand, largeText, voice, createdAt }
Session          { id, date, gameId, courseId, domain, level, score, accuracy, durationSec }
                   - gameId:   'shopping' | 'cooking' | 'wayfinding' | 'medication'
                   - courseId: 코스 플레이면 코스 id, 낱개 플레이면 null
RealWorldMission { id, date, text, done }
AppState         { schemaVersion, profile, sessions[], missions[], levels{} }
```

- `schemaVersion` 필드를 두고, 최초 실행 시 **데모용 더미 데이터 시드**(지난 2주치 세션·실행 미션 몇 개)를 채워 가족 리포트가 바로 의미 있게 보이도록 한다.
- 설정에서 "데이터 초기화" 제공.

---

## 10. 디자인 방향

- 따뜻하고 신뢰감 있는 톤. 주색은 차분한 네이비/블루 계열 + 부드러운 포인트 컬러. 배경은 밝고 깨끗하게.
- 낙인감 없는 밝은 분위기("치매" 단어를 UI 전면에 쓰지 말 것 — "두뇌 건강", "오늘의 활동" 등).
- 큰 카드, 둥근 모서리, 넉넉한 여백. 아이콘은 크고 명확하게.
- **다크모드는 구현하지 않는다.** 고령자 대상 앱에서 우선순위가 낮고, 스타일 작업량이 두 배가 된다.

---

## 11. 빌드 순서 (마일스톤)

**마일스톤 7까지가 "완성된 앱"이다.** 8~10은 완성된 앱 위에 얹는 확장이므로, 하지 않아도 결과물이 온전하다.

| # | 내용 |
|---|---|
| 1 | **셋업** — Vite+React+Tailwind v3.4+라우터+Zustand 스캐폴딩, 접근성 디자인 토큰, 하단 탭 내비, 빈 화면 라우팅 |
| 2 | **홈 + 온보딩 + 설정** + `storage.js` + 더미 데이터 시드 + **웰니스 고지** |
| 3 | **게임 1 (장보기)** + `recipes.js` + 세션 저장 + 결과 화면 + 실행 브리지 미션 |
| 4 | **게임 2 (요리 레시피)** + 실행 브리지 연동 |
| 5 | **적응형 난이도 + 기능 프로파일 차트** |
| 6 | **가족 모드 리포트 대시보드 (Recharts)** |
| 7 | **PWA + 접근성 점검**(글자크기·대비·터치·키보드·aria) + README |
| — | *— 여기까지가 완성된 앱 —* |
| 8 | 게임 3 (길찾기) |
| 9 | 코스 모드 "장 보러 가는 날" (6.5) |
| 10 | 게임 4 (복약) |

**각 마일스톤 후 앱이 항상 실행 가능한 상태를 유지하라.** `npm run dev`로 바로 확인되어야 한다.

### 11.1 마일스톤 진행 규칙

- 한 세션에 **마일스톤 하나만** 구현한다. 앞서가지 않는다.
- 완료를 보고하기 전에 **직접 실행해서 확인**한다. 확인 없이 "완료"라고 하지 않는다.
- 마일스톤이 끝나면 커밋한다. 커밋 메시지 형식: `feat: 마일스톤N - 요약`

---

## 12. 비목표 (하지 말 것)

- 백엔드·서버·실제 계정/로그인·클라우드 동기화 (localStorage만).
- TypeScript.
- 실제 의료 진단·점수의 의학적 해석·질병 예측.
- 결제·구독 결제 흐름.
- 외부 지도/유료 API.
- 드래그 앤 드롭.
- 다크모드.
- 3D 그래픽 (원근·회전은 고령자의 터치 정확도와 인지 부하에 불리하다).
- 과도한 애니메이션·화려한 이펙트(고령자 혼란 유발).
- 지정된 마일스톤 범위를 넘어서는 구현.

---

## 13. 산출물

- 실행 가능한 React 웹앱 소스 전체.
- `README.md`: 실행법(`npm install && npm run dev`), 폴더 구조, 각 게임·데이터 모델 설명, 웰니스 고지, "이후 확장 아이디어(백엔드·DTx 전환 등)" 섹션.
- 깔끔한 폴더 구조: `src/pages`, `src/games`, `src/components`, `src/lib`, `src/store`.
